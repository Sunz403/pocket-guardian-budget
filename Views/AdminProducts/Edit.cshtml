@model AIShoppingAssistant.Models.Product
@{ ViewData["Title"] = "Edit Product"; }
<div class="mx-auto max-w-3xl">
    <a asp-action="Index" class="text-sm font-semibold text-brand-700 hover:text-brand-900">← Back to products</a>
    <section class="mt-4 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8">
        <h1 class="text-2xl font-bold text-slate-900">Edit product</h1>
        <p class="mt-2 text-sm text-slate-500">Update the catalogue information for @Model.Name.</p>
        <form asp-action="Edit" method="post" enctype="multipart/form-data" class="mt-7 space-y-6">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="Id" />
            <partial name="_ProductForm" model="Model" />
            <div class="flex justify-end gap-3"><a asp-action="Index" class="btn btn-secondary">Cancel</a><button class="btn btn-primary" type="submit">Save changes</button></div>
        </form>
    </section>
</div>
@section Scripts { <partial name="_ValidationScriptsPartial" /> }
